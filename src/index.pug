include UI/header/header.pug 
include UI/Cards/find-room/find-room.pug
include UI/footer/footer.pug

doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Главная страница
    body 
        .landing
            +header({addClass:"landing__header", name:""})
            main.landing__body
                .landing__content
                    +find-room({addClass:"landing__find-room"})

        +footer({addClass:"landing__footer"})



            
            
            